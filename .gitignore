# secrets — never commit
.env

# dependencies (none today; defensive)
node_modules/

# collector output — intentionally TRACKED (not ignored): GitHub Actions commits these
# back to main every run so the rolling history (cockpit-history.json) accumulates across
# ephemeral runners. See .github/workflows/collect-and-deploy.yml.
# Locally `npm run collect` regenerates them in place.

# retired v1 panel — archived on disk, kept out of this repo
_legacy/

# logs / OS
*.log
.DS_Store
Thumbs.db
