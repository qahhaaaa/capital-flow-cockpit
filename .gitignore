# secrets — never commit
.env

# dependencies (none today; defensive)
node_modules/

# collector output — regenerated by `npm run collect`
public/data/cockpit.json
public/data/cockpit-history.json

# retired v1 panel — archived on disk, kept out of this repo
_legacy/

# logs / OS
*.log
.DS_Store
Thumbs.db
